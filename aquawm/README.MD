# This is not the main README file of the project, so this will only talk about compilation:

# To compile AquaWM, install dependencies:
`libx11-dev feh`
# After this write:
`make` and `sudo/doas make install`
